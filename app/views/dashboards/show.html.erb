<div class="container mx-8 my-4 px-4 py-4 bg-white border border-light">
  <h1 class="pb-4">Hi, Nurra </h1>

  <div class="col mb-4">
    <h3>Requests</h3>

    <ol class="list-group list-group-flush">
      <% @bookings.each do |booking| %>
      <li class="list-group-item d-flex justify-content-between align-items-start">
        <div class="fw-light">Wish to walk : <%= booking.pet.name %> - Date of booking : <%= booking.booking_date %> - From : <%= booking.user.email %></div>
          <div>
              <%= simple_form_for(booking) do |f| %>
              <%= f.input :status, collection: ["pending", "accepted", "denied", "completed"], input_status_html: {class: "form-select"} %>
              <%= f.submit class: "btn btn-dark btn-sm rounded-pill" %>
              <%# <button class="btn btn-warning btn-sm text-dark rounded-pill" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
                pending
              </button> %>
              <%# <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1"> %>
                <%# <li><a class="dropdown-item" href="#">accepted</a></li> %>
                <%# <li><a class="dropdown-item" href="#">denied</a></li> %>
                <%# <li><a class="dropdown-item" href="#">completed</a></li> %>
                <% end %>
          </div>
      </li>
      <% end %>
    </ol>
  </div>
</div>

<%# class="btn btn-warning btn-sm text-dark rounded-pill">Pending</span> %>
<%# <span class="btn btn-outline-success btn-sm rounded-pill">Accepted</span> %>
<%# <span class="btn btn-outline-danger rounded-pill">Denied</span> %>
<%# <span class="btn btn-light text-dark rounded-pill">Completed</span> %>
