<body>
    <div class="content">
      <div class="cards">
        <div class="card" style="background-color: #F7F1E3">
          <%= @pets.first.name %>
        </div>

        <div class="card" style="background-color: #F7F1E3">
          TODO: iterate through Pet.all showing the TOP n cards
        </div>

        <div class="card" style="background-color: #F7F1E3">
          <%= @pets.last.name %>
        </div>

        <div class="card" style="background-color: #F7F1E3">
          <%= @pets.first.name %>
        </div>

        <div class="card" style="background-color: #F7F1E3">
          <%= @pets.last.name %>
        </div>

        <div class="card" style="background-color: #F7F1E3">
          <%= @pets.first.name %>
        </div>

      </div>

      <div id="map" style="width: 100%; height: 600px;"
        data-controller="map"
        data-map-markers-value="<%= @markers.to_json %>"
        data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
      </div>
      <%# <img id="map" src='https://raw.githubusercontent.com/lewagon/layouts-demo/gh-pages/images/mapbox.png'> %>
    </div>

</body>
